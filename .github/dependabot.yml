version: 2
updates:
  # Actions are pinned by commit SHA; this keeps the pins and their version
  # comments current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
